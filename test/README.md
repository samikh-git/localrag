This file should be used to test the package. It is a toy codebase for the backend of a weather app.

It has a suboptimal design so feel free to test the LLM on its capabilities.

Run `localrag init` to start this as a localrag project.